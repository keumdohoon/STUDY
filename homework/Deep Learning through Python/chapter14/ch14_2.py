import pandas as pd

df = pd.read_csv('./data/csv/winequality-white.csv')

print(df) #[4898 rows x 1 columns]


#14_4 csv라이브러리로 csv만들기 csv불러오기 14_5
import csv


#with 문을 사용하여 파일을 처리합니다.
with open("csv0.csv", "w")as csvfile:
    #writer() 매서드의 인수로 csvfile과 개행 코드(/n)를 지정합니다.
    writer = csv.writer(csvfile, lineterminator="/n")

    #writerow(리스트)로 행을 추가합니다. 
    writer.writerow(["city", "year", "season"])
    writer.writerow(["Nagano", "1998", "winter"])
    writer.writerow(["Sydney", "2000", "summer"])
    writer.writerow(["Salte Lake City", "2002", "winter"])
    writer.writerow(["Athens", "2004", "summer"])
    writer.writerow(["Torino", "2006", "winter"])





















































































































































































































































